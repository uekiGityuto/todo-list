# 言語

日本人が中心のプロジェクトのため、コード内のコメントやドキュメントは原則日本語で書くこととする。
また、日本人向けのサービスであるため、ユーザーに表示されるエラーメッセージも日本語で書くこととする。

## コメントは日本語で書く

- 文章のコメントは日本語で統一する
- キーワード的なコメント（`// Given` / `// When` / `// Then` 等）は英語のまま可
